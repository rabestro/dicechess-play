// SPA mode: fully client-side rendered (adapter-static with an index.html fallback).
// The game runs the Scala.js engine in the browser; there is no server to render on.
export const ssr = false;
export const prerender = false;
